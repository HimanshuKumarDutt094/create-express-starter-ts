# Database
DATABASE_URL="./dev.db"

# Server
PORT=3000
NODE_ENV=development

# CORS
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:5173,https://example.com
CORS_ORIGINS=

# Authentication
BETTER_AUTH_SECRET="change-me-32+chars"
BETTER_AUTH_URL=http://localhost:3000 # Base URL of your app (override in production)

# Rate limiting: template includes `express-rate-limit` defaults. Tune via env in future.
BETTER_AUTH_GITHUB_CLIENT_ID=dummy
BETTER_AUTH_GITHUB_CLIENT_SECRET=dummy

# Valkey (Redis) Configuration
VALKEY_HOST=127.0.0.1
VALKEY_PORT=6379
VALKEY_TLS=false  # Set to 'true' for TLS connections
VALKEY_USERNAME=  # Optional
VALKEY_PASSWORD=  # Optional

# Cache TTL (in seconds)
USER_CACHE_TTL=300  # 5 minutes

# Logging
LOG_LEVEL=info
